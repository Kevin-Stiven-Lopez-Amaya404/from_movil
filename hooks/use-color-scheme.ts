/**
 * Re-export simple color scheme hook para uso directo en toda la app.
 *
 * Este modulo agrupa la llamada al hook de React Native, permitiendo que
 * las importaciones sean consistentes y, si es necesario, aplicar ajustes
 * cross-platform o mocks en un solo lugar.
 */
export { useColorScheme } from 'react-native';
